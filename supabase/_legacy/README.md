# Legacy Supabase Migrations

Questa cartella contiene import storici delle migrations da due repository separati:

- `kalos-app/` - Migrations dall'app Expo clienti
- `kalos-app-management/` - Migrations dal gestionale Vite

Questi sono stati importati per confronto con la migration history remota e stabilire un set canonico. Non modificare questi file direttamente.

Il set canonico è in `../migrations/`.

